# This file ensures the data/ directory is tracked by Git.
# The actual runtime files (csrb.db, memory.json) are excluded via .gitignore
# and will be created automatically when you run the application for the first time.
